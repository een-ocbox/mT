name: blank

on:
  push:

jobs:
  definition:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        peak: ["internship", "shave", "dump", "drink", "bloody"]
        recognize: ["vitally", "art", "conclude", "helpless"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar va
